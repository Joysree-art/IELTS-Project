name: project_one
description: A Flutter project for IELTS app
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  audioplayers: ^6.1.0
  http: ^1.2.2
  flutter:
    sdk: flutter

  speech_to_text: ^7.2.0
  permission_handler: ^11.3.1
  supabase_flutter: ^2.8.0

  cupertino_icons: ^1.0.8
  image_picker: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
assets:
  - assets/audio/